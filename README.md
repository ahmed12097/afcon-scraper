# AFCON Scraper (Selenium) + GitHub Actions

This repo runs a Selenium-based scraper on a schedule (GitHub Actions) and writes CSV outputs to `data/`.
You can publish the `data/` folder via GitHub Pages and connect Power BI to the CSV URLs.

## Quick start
- Push to GitHub
- Actions → Run workflow
- Settings → Pages → Deploy from branch → main → /data
